# LibraryFlow — environment variables template
# Copy this file to .env and replace values for your local setup.
# Never commit your real .env (it is git-ignored).

# SQL Server SA password.
# Required complexity: min 8 chars, with uppercase, lowercase, number, and symbol.
MSSQL_SA_PASSWORD=ChangeMeStrong!2026
